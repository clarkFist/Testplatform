# VCU测试开关桩管理工具依赖包

# 核心依赖
# Python标准库已包含：tkinter, socket, threading, logging, json, pathlib

# 可选的增强功能（按需安装）
# pillow>=9.0.0            # 如需图标和图像支持
# matplotlib>=3.5.0        # 如需数据可视化功能
# numpy>=1.21.0           # 如需数值计算功能

# 必需依赖（YAML配置支持）
PyYAML>=6.0               # 用于YAML配置文件

# 开发和测试依赖（开发环境可选）
# pytest>=7.0.0          # 用于单元测试
# pytest-cov>=3.0.0      # 用于测试覆盖率分析
# black>=22.0.0           # 用于代码格式化
# flake8>=4.0.0           # 用于代码质量检查

# 注意：本应用程序主要基于Python标准库构建
# 无需额外依赖即可正常运行 